# 의존성 버전 고정(security L2). 운영 재현성 + 취약 버전 유입 방지.
fastapi==0.136.3
uvicorn[standard]==0.48.0
fonttools==4.63.0
brotli==1.2.0
requests==2.34.2
pydantic==2.13.4
# 테스트(TestClient)용. 운영 런타임에는 불필요하나 핀해 둔다.
httpx==0.28.1
pytest==8.4.2
