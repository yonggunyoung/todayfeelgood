{
  "name": "geurim",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "그림공장 — AI 이미지 배치 생성 스튜디오 (본인 OpenAI·Gemini 키, 빌드 불필요 정적 PWA)",
  "scripts": {
    "start": "python3 -m http.server 8080",
    "proxy": "node proxy.mjs",
    "test": "node --test"
  }
}
