{
  "name": "naengbiseo-toss",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "냉비서 — 앱인토스 WebView 미니앱 (Vite로 바닐라 PWA를 래핑)",
  "scripts": {
    "vendor": "node scripts/vendor.mjs",
    "dev": "yarn vendor && ait dev",
    "build": "yarn vendor && ait build",
    "deploy": "ait deploy"
  },
  "dependencies": {
    "@apps-in-toss/web-framework": "2.9.3"
  },
  "devDependencies": {
    "vite": "^5.4.0",
    "typescript": "^5.5.0"
  },
  "packageManager": "yarn@1.22.22"
}
